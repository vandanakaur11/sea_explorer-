const { userModel } = require('./user.model');
const { nftModel } = require('./nftCollections.model');
const { thumbnailModel } = require('./nft.thumbnail');
module.exports = {
    userModel,
    nftModel,
    thumbnailModel,
}
